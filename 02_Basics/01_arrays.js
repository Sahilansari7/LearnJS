//arrays

const myArr = [0, 1, 2, 3, 4, 5];
const myNickanames = ["Sahil", "Zein", "Enishi"];
const myArr2 = new Array(1, 2, 3, 4); //another way to put arrays
//console.log(myArr[1]); find position

//array Methods

// myArr.push(6,7,"sahil")  //push helps to add the Element
// console.log(myArr);

// myArr.pop() //pop removes the last element
// console.log(myArr);

// myArr.unshift(9) //unshift add element to first position
// myArr.shift() //remove element from that first position
// console.log(myArr);

console.log(myArr);

//slice or splice

/* 

The slice() method can be used to create a copy of 
an array or return a portion 
of an array. It is important to note that the slice() 
method does not alter the original array but instead
 creates a shallow copy.
 
 the splice() method will change the contents of the original array. 
 The splice() method is used to add or remove elements of an existing array and 
 the return value will be the removed items from the array.

 */
